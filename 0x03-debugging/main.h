#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>

void positive_or_negative(int i);
int largest_number(int __attribute__((unused)) a, int __attribute__((unused)) b, int __attribute__((unused)) c);
int convert_day(int month, int day);
void print_remaining_days(int month, int day, int year);

#endif
